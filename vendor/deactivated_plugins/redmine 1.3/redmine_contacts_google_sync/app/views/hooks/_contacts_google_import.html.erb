<% if authorized_for_permission?(:import_contacts, @project) %>
  <h3><%= l(:label_import) %></h3>
  
  <table class="list">
		<thead><tr>
		<th><%= l(:label_import) %></th>  
		</tr></thead>
		<tbody> 
	  
			<tr class="<%= cycle 'odd', 'even' %>">
			<td>
		      <%= link_to l(:label_contact_google_sync), { :controller => 'google_contacts', :action => 'index', :project_id => @project} %>
		    </td>
			</tr>   
	    </tbody>
	</table>  
<% end %>  